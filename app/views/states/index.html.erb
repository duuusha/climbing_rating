<header>
  <h1>
    States of <%= link_to(@country.name, @country) %>
  </h1>
</header>

<ul id="states">
  <% @states.each do |state| %>
    <li>
      <%= link_to(state.name) %>
    </li>
  <% end %>
</ul>